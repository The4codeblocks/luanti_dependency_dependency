# dependency_dependency
Luanti helper "mod" to allow mods to load after every other mod

## Use guide
Include this "mod" in your mod's `optional_depends`, and Luanti's dependency load order manager will do the rest
